{
  "name": "wix-hubspot-integration",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hosted Wix to HubSpot integration prototype for bidirectional contact sync and lead capture.",
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
